name: CI-smoke
on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/ci-smoke.yml"
permissions:
  contents: read
jobs:
  smoke:
    runs-on: [self-hosted, macOS, ARM64]
    steps:
      - run: echo smoke-ok-from-main
